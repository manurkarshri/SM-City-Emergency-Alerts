export const $=s=>document.querySelector(s);export const html=t=>{const d=document.createElement("div");d.textContent=String(t||"");return d.innerHTML};export const attr=t=>String(t||"#").replace(/'/g,"%27").replace(/"/g,"%22");export async function fetchTimeout(url,ms=6500){const c=new AbortController();const timer=setTimeout(()=>c.abort(),ms);try{const r=await fetch(url,{signal:c.signal,cache:"no-store"});clearTimeout(timer);return r}catch(e){clearTimeout(timer);throw e}}export function fmtDate(v){const d=new Date(v);return isNaN(d)?"Recent":d.toLocaleTimeString("en-IN",{hour:"2-digit",minute:"2-digit"})+" | "+d.toLocaleDateString("en-IN",{month:"short",day:"numeric"})}export function recent(v,h){const t=new Date(v).getTime();return isNaN(t)||Date.now()-t<=h*3600000}export function syncText(){return"Last synced: "+new Date().toLocaleTimeString("en-IN",{hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:true})}export function validityText(a){const end=a.expiresAt||a.validUntil;if(!end)return"";const d=new Date(end);if(isNaN(d))return"";return"Active till "+d.toLocaleDateString("en-IN",{day:"numeric",month:"short"})+" "+d.toLocaleTimeString("en-IN",{hour:"2-digit",minute:"2-digit"})}export function isActiveAlert(a,h){const now=Date.now();const start=a.startsAt?new Date(a.startsAt).getTime():null;if(start&&!isNaN(start)&&now<start)return false;const end=a.expiresAt||a.validUntil;if(end){const t=new Date(end).getTime();return isNaN(t)?recent(a.pubDate,h):now<=t}if(a.durationHours){const base=new Date(a.pubDate).getTime();return isNaN(base)?true:now<=base+(Number(a.durationHours)||h)*3600000}return recent(a.pubDate,h)}