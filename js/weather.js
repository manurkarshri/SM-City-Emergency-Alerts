import{fetchTimeout}from"./utils.js";export async function loadWeather(zone){try{const url=`https://api.open-meteo.com/v1/forecast?latitude=${zone.lat}&longitude=${zone.lon}&current=temperature_2m,relative_humidity_2m,precipitation,rain,weather_code,wind_speed_10m,wind_gusts_10m&hourly=precipitation_probability,visibility&forecast_days=1&timezone=auto`;const r=await fetchTimeout(url,5000);if(!r.ok)throw new Error("weather");const d=await r.json();const c=d.current||{};const h=d.hourly||{};const i=Array.isArray(h.time)?h.time.findIndex(t=>t.slice(0,13)===(c.time||"").slice(0,13)):0;return{temp:c.temperature_2m,humidity:c.relative_humidity_2m,rain:c.rain||c.precipitation||0,wind:c.wind_speed_10m,gust:c.wind_gusts_10m,code:c.weather_code,rainChance:Array.isArray(h.precipitation_probability)?h.precipitation_probability[Math.max(i,0)]:null,visibility:Array.isArray(h.visibility)?Math.round((h.visibility[Math.max(i,0)]||0)/1000):null}}catch{return null}}export function weatherText(code){if([95,96,99].includes(code))return"Thunderstorm";if([61,63,65,80,81,82].includes(code))return"Rain";if([51,53,55].includes(code))return"Drizzle";if([45,48].includes(code))return"Fog";if([1,2,3].includes(code))return"Cloudy";if(code===0)return"Clear";return"Weather"}